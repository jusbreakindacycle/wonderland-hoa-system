# dependencies
node_modules/

# build output
dist/
dist-ssr/

# Stage 1 mobile — Expo scaffold seed and CI bundle output are never committed
.stage1-expo-seed/
mobile/dist-ci/

# env files — never commit real credentials
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Supabase CLI local artefacts — never commit
# supabase/config.toml IS tracked; only the CLI's local state is not.
supabase/.temp/
supabase/.branches/
